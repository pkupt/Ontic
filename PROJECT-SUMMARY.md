# Ontic 工程全景总结

> 生成时间：2026-08-14 ｜ 状态：S0–S7 核心 + M1–M13 + A–K 系列增强 全部落地并验证

---

## 一、一句话定位

**开源版 Palantir Foundry 的最小可用复刻**：本体（Ontology）驱动 —— 导入一张表 → 定义对象 → OSDK 类型安全查询 → 动作写回业务对象 → 前端/Agent 直接操作业务语义。**自研重心压在本体层**（差异化护城河），其余层用开源拼装。

## 二、技术栈

| 层 | 选型 |
|---|---|
| 后端 | Python 3.13 + FastAPI（31 模块 / **7,382 行**） |
| 数据平面 | DuckDB（列式分析，58 张 ont__ 表）+ SQLite（元数据） |
| 前端 | 原生 JS SPA（app.js 2,961 行 + styles 186 + index 65，**零框架依赖**） |
| 认证 | PBKDF2 + JWT（stdlib，零第三方加密依赖） |
| 部署 | Docker Compose / Dockerfile / Makefile，MIT License |

## 三、现状数据（实测）

| 指标 | 数值 |
|---|---|
| 后端 REST 端点 | **159+** |
| 对象类型 / 应用 / 管道 | 20 / 11 / 3 |
| 动作 / Chatbot / 分析 | 63 / 2 / 2 |
| 用户（admin/analyst/demo） | 3（ABAC 授权） |
| 演示数据 | customer/order/product/supplier/purchase_order/city 等 120+ 对象 |

## 四、四层能力架构

```
┌────────────────────────────────────────────────────┐
│ 应用层  6 类应用 + 跨应用交互 + 组件 + Contour 分析  │
│ 智能层  AIP 分析师 + Chatbot Studio + RAG + OMCP    │
│ 本体层★ 对象/属性/链接/动作 + SQL 下推 + OSDK + ABAC │
│ 数据层  连接器 5 类 + 管道 + 快照/分支 + 血缘 + 时序 │
└────────────────────────────────────────────────────┘
★ = 自研护城河（80% 价值）
```

## 五、功能模块矩阵

| 域 | 已落地能力 |
|---|---|
| **本体** | 对象类型（属性约束：必填/枚举/正则/敏感）、链接（外键/多跳图）、动作引擎（CRUD+自定义+审批流）、查询 SQL 下推、对象抽屉（卡片+放射图+时间线）、导出/导入/克隆 |
| **数据** | 连接器（CSV/JSON/Parquet/REST/PG，自动注册类型）、管道构建器（SQL/Python 多步）、快照/检查点/分支、血缘（表级+类型级）、SQL 工作台、时空查询、时间序列、媒体、Contour 分析画布（参数化+存为新类型） |
| **智能** | 分析师（Threads 会话+链式查询+自动图表+可选 LLM）、**Chatbot Studio**（指令+工具集白名单+参数变量+会话日志+RAG 知识库）、OMCP 工具平面（MCP/JSON-RPC）、评估套件、模型目录/对比、文档智能 |
| **应用** | Form/Dashboard/View/Workflow/**Kanban** 五类应用、版本快照对比、跨应用对象定位、字段筛选组件、CRUD 自动生成 |
| **治理** | 用户 CRUD/角色、ABAC（类型级+动作级拦截）、**敏感字段脱敏**（非 admin 掩码）、审批流、安全标记、敏感扫描、留存策略、**TTL 生命周期**、连接凭据加密存储 |
| **运维** | 监控规则、自动动作触发器、深层健康检查、活动日志/审计、项目空间（多项目切换） |
| **开发者** | OSDK（Python 生成）、API 令牌、自定义端点、API 参考、市场（包安装）、Python 转换 |

## 六、迭代里程碑

| 阶段 | 内容 |
|---|---|
| **S0–S7**（MVP） | 本体核心（解析器/动作引擎/OSDK）→ 接入管道 → 应用构建 → AIP → 治理 → 发布（Docker/MIT） |
| **M1–M13**（Foundry 对齐） | 链接/图探索/时序/媒体/审批/OSDK/血缘/评估/审批流/时空/幂等/快照检查点/管道 |
| **A–K 系列**（深化） | A1-3 会话/链式/图探索 → E1-2 事件触发器/看板 → F1 跨应用交互 → **H1-4 Chatbot Studio 全套** → I1-2 Contour → J1-3 TTL/组件/凭据加密 → **多项目空间** |
| **质量闭环** | 端到端 9 环节验证（21 PASS）→ 全量 159 端点验证（42 PASS）→ 11 项缺口全修复 |

## 七、质量与验证

- **端到端闭环**：本体创建→数据加载→灌入→实例化→可视化→智能体→消费→动作→回写，**21/21 PASS**
- **全量覆盖**：159 路由 / 13 能力组动态实测（用户权限管理全 PASS：ABAC 拦截 403、授权生效、敏感字段脱敏掩码）
- **已修复缺口**：4 类资源删除端点、应用编辑保存、parameters 校验、留存 KeyError、TS 删除、清空数据、Omnibar 历史、媒体删除
- **验证工具**（可回归复用）：`data/scan_ui.py`（死链扫描）、`data/scan_crud.py`（CRUD 闭环）、`data/full_verify.py`（全量 13 组）、`data/e2e_verify.py`（端到端）

## 八、对标 Palantir

**已对齐**：本体全链路（SQL 下推/链接图/动作引擎/OSDK/OMCP）、对象抽屉、看板、审批、事件触发器、跨应用交互、Contour 分析、Chatbot Studio、多项目。
**差距（见 ROADMAP-NEXT.md）**：RAG 向量检索、对象级权限、字段级 AES-GCM、TTL 调度、Workshop 画布拖拽、OSDK 多语言、196 连接器、SSO/多租户（P3 不做）。

## 九、演示路径（上手 5 分钟）

1. **首页**：点"🔌 接入数据"→ 连接器上传 CSV → 自动生成对象类型+CRUD 动作
2. **本体**：打开 purchase_order → 属性/动作/链接 tab（关系图悬停看对象数、数据 tab 看板）
3. **应用**：打开 po_board（看板按状态分列）→ 头部说明 → 点卡片看详情
4. **AIP**：分析师"查 purchase_order"；Chatbot Studio 创建自定义 Agent
5. **治理**：管理 → 用户授权、角色下拉；类型概览"近 7 天活动趋势"（真实日志）

## 十、路线图

```
P0 闭环补全（已完成 ✅）→ P1 深化（RAG 向量化/Agent 工具编排/对象级权限/字段级加密/TTL 调度/数据集版本管理）
→ P2 体验（Workshop 画布/OSDK TS/告警通道/组件市场）→ P3 明确不做（SSO/多租户/水平扩展/K8s）
```

---

**工程结论**：核心价值（本体层）已完整闭环，四层能力从"导入一张表"到"智能体消费+动作回写"全链路可用；质量经过全量验证与修复；剩余工作是深化（P1）与体验打磨（P2），路线图见 ROADMAP-NEXT.md。
