# Ontic 生产就绪评估

> 生成时间：2026-08-14 ｜ 评估方式：全量验证结果 + 代码核查 + 部署现状

---

## 一、结论（三档）

| 场景 | 是否可上线 | 说明 |
|---|---|---|
| **内部团队试用 / Demo / 教学 / POC** | ✅ **现在就可以** | 功能闭环完整、全量验证通过 |
| **小规模生产**（<50 并发、单实例、可信环境） | ⚠️ 补 8 项 P0 后（约 1–2 周） | 安全/备份/Docker 验证是硬门槛 |
| **正式对外 / 多租户 / 高可用** | ❌ 不建议 | 需 SSO/对象级权限/水平扩展（超出当前定位，见 P3） |

**一句话**：当前是"功能完备的生产可用原型"——**演示与内部试用可直接上**；对外生产需先补生产化清单。

## 二、就绪度评分

| 维度 | 评分 | 现状 |
|---|---|---|
| 功能完备度 | ★★★★☆ | 四层能力闭环，159 端点全量验证 |
| 认证与授权 | ★★★☆☆ | JWT+PBKDF2+ABAC+字段脱敏 ✓；**密码无复杂度策略** |
| 密钥与配置 | ★★★☆☆ | Secret/CORS 支持 env ✓；**默认 secret 需生产覆盖** |
| 数据持久化 | ★★☆☆☆ | DuckDB 单文件+SQLite；**无备份策略/恢复演练** |
| 性能与并发 | ★★☆☆☆ | 单进程 uvicorn；DuckDB 写锁；**未做压测/限流** |
| 可观测性 | ★★★☆☆ | 健康检查/活动日志 ✓；**无指标采集/结构化日志** |
| 部署 | ★★★☆☆ | Docker 编排齐全；**Docker 未实机构建验证** |
| 工程化 | ★★☆☆☆ | 有验证脚本 ✓；**无 CI 流水线/自动化测试接入** |

## 三、生产化缺口清单（分级）

### 🔴 P0 · 必须（小规模生产前补）
| # | 项 | 行动 |
|---|---|---|
| 1 | **Docker 实机验证** | 真实构建镜像 + compose 起服务 + 端到端冒烟（当前仅校验配置） |
| 2 | **备份与恢复** | 定时备份 DuckDB/SQLite（cron/脚本）+ 恢复演练 + 文档 |
| 3 | **强制 secret** | 启动时校验 `ONTIC_SECRET_KEY` 非默认值，否则拒绝启动（fail-closed） |
| 4 | **密码策略** | 创建用户/改密时校验长度≥8 + 复杂度 |
| 5 | **HTTPS 反代** | 提供 nginx/Caddy 示例配置（TLS 终结 + 转发） |
| 6 | **限流与防护** | 登录限流（防爆破）、API 基础限流 |
| 7 | **DuckDB 写并发提示** | 单写者约束文档化 + 连接串行化（避免多进程写锁） |
| 8 | **启动安全校验** | 首次启动强制改 admin 密码 |

### 🟠 P1 · 建议（小规模生产稳定运行）
| # | 项 | 说明 |
|---|---|---|
| 9 | 字段级加密升级 | 轻量 XOR → AES-GCM（密钥 env 注入） |
| 10 | 结构化日志 + 指标 | JSON 日志 + /metrics（Prometheus 格式） |
| 11 | CI 流水线 | GitHub Actions：lint + 验证脚本（scan_crud/full_verify）自动跑 |
| 12 | 操作审计增强 | 关键操作（授权/删类型/改角色）写不可变审计表 |
| 13 | 数据迁移机制 | 版本化 schema 迁移（当前 try/except ALTER 脆弱） |

### 🟡 P2 · 可选（正式对外）
- SSO/LDAP、对象级权限、多租户隔离、水平扩展、K8s 部署、Spark Transforms

### ⛔ 明确不做（超出单机定位）
- 多租户、水平扩展、K8s、Spark——见 ROADMAP-NEXT.md P3

## 四、上线路径（建议）

**阶段 0（立即）· 内部试用**：现状即可 → 团队试用收集反馈
**阶段 1（约 1–2 周）· 小规模生产**：P0 8 项 → Docker 实机验证 → 备份就位 → 交付运维手册
**阶段 2（按需）· 对外**：P1 4 项 → 审计/指标 → 评估是否值得投入 P2

## 五、风险提示（诚实清单）

1. **数据安全**：DuckDB 明文落盘（未加密磁盘），敏感数据需磁盘加密层
2. **并发上限**：未压测，估算 <50 并发安全；超此需架构演进
3. **Docker 未验证**：本地运行 OK ≠ 镜像构建 OK，必须先实机验证
4. **单点**：单实例无故障转移，适合"可信环境内的业务系统"，不适合 7×24 关键业务
