# Ontic 迭代方向梳理（对标 Palantir Foundry）

> 生成时间：2026-08-14 ｜ 依据：foundry_docs 14 大分区 / 1408 篇 ｜ 当前覆盖率 ✅32 / 🟡22 / ⬜14
> 状态：**仅梳理，待执行**

---

## 一、对标全景（Palantir → Ontic 差距）

| Palantir 能力 | Ontic 现状 | 差距 | 优先级 |
|---|---|---|---|
| **本体层**（护城河） | 类型/属性/链接/动作/SQL下推/OSDK/OMCP ✅ | 对象级权限（当前类型级 ABAC） | P1 |
| **AIP** | 分析师 + Chatbot Studio(H1-4) + OMCP ✅ | RAG 关键词打分→向量检索；Agent 工具集扩展（写回/审批）；多 Agent 协作 | P1 |
| **数据平面** | 连接器 5 类 + 管道 + 快照/检查点 + Contour(I1/I2) ✅ | 数据集版本管理深化（Foundry 数据集浏览器）；196 连接器按需扩 | P1 |
| **应用层** | 6 类应用 + 跨应用交互 + 自定义组件(J2) ✅ | Workshop 画布式拖拽（当前表单式配置）；组件市场 | P2 |
| **治理** | ABAC + 审批 + TTL(J1) + 凭据加密(J3 轻量) ✅ | 字段级加密升级（XOR→AES-GCM）；TTL 自动化调度；合规报表 | P1 |
| **运维** | 血缘 + 监控 + 事件触发器(E1) + 健康检查 ✅ | 监控告警通道（webhook/通知）；多环境 | P2 |
| **开发者** | OSDK(Python) + API 令牌 + 市场 ✅ | OSDK 多语言；代码仓库/CI；Webhook | P2 |
| **平台** | 多项目/空间 ✅（资源分组） | 项目级权限隔离；SSO/LDAP；多租户；水平扩展 | P3 |

## 二、下一阶段迭代方向（推荐序）

### P0 · 闭环补全（本轮验证发现）
1. **CRUD 补 D**：对象类型/动作/链接/管道删除端点 + 前端删除按钮（含确认）
2. **应用编辑保存**（补 U）：编辑表单 → 保存 → 版本快照
3. **动作创建 parameters 校验**（400 而非 500）
4. **对象类型清空数据**（truncate）

### P1 · 深化（对标 Palantir 核心）
5. **RAG 升级**：知识库向量化检索（当前关键词打分），Chatbot 回答质量提升
6. **Agent 工具编排扩展**：Chatbot 支持写回工具（execute_action 已有）、审批工具、多 Chatbot 协作
7. **数据集版本管理深化**：数据目录（按数据集浏览 + 血缘 + 快照时间线）
8. **字段级加密升级**：XOR → AES-GCM（当前是防静态泄露的轻量级）
9. **TTL 自动化调度**：定时应用策略（复用 E1 事件触发器机制）
10. **对象级权限**：grant 细化到对象实例（当前类型级）

### P2 · 体验与扩展
11. **Workshop 画布式应用构建**：拖拽布局（对齐 Workshop）
12. **OSDK 多语言**：TypeScript SDK（当前 Python）
13. **监控告警通道**：命中 → 企业微信/邮件通知（webhook）
14. **组件市场**：可分享自定义组件

### P3 · 明确不做（超出本地单机范围）
- SSO/LDAP、多租户、水平扩展、K8s 部署、Spark Transforms、代码仓库/CI
- 196 连接器全量（按注册表模式按需扩展）

## 三、优先级理由

- **P0 优先**：闭环缺口影响"可用性"——用户建了删不掉是硬伤，先补
- **P1 优先深化本体/AIP/治理**：这三层是 Foundry 差异化价值所在，也是 Ontic 护城河
- **P2 是锦上添花**：画布拖拽/多语言 SDK 提升开发者体验
- **P3 明确划线**：本地单机定位，不追企业级设施

## 四、执行节奏建议

```
第 1 批（闭环补全）: P0-1 ~ P0-4（删除闭环）+ 应用编辑保存 + parameters 校验 + truncate
第 2 批（AIP 深化）: RAG 向量检索 + Agent 工具编排 + 对象级权限
第 3 批（数据深化）: 数据集版本管理 + 数据目录 + TTL 调度 + 字段级加密
第 4 批（体验扩展）: Workshop 画布 + OSDK TS + 告警通道 + 组件市场
```
