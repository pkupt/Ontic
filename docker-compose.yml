services:
  ontic:
    build: .
    ports:
      - "8080:8000"
    env_file:
      - .env
    environment:
      - ONTIC_DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
